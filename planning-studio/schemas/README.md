# Schemas

These draft JSON Schemas test whether Planning Studio records can be both
human-readable and machine-indexable.

They are intentionally permissive during design review. Tightening a schema is
appropriate only after real captures expose which fields are stable and which
were guessed prematurely.

