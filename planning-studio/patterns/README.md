# Patterns

This directory is reserved for extracted portions that may be reusable across
domains or initiatives.

Possible states are `candidate`, `validated`, and `rejected`. A pattern is not
called reusable until it succeeds in a materially different context.

No patterns have been extracted yet.

