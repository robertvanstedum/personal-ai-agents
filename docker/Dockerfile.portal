FROM python:3.12-slim

WORKDIR /app

COPY docker/requirements.portal.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV FLASK_ENV=production
ENV PORT=5001

EXPOSE 5001

CMD ["python", "minimoi_portal/app.py"]
