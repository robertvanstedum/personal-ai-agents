FROM python:3.12-slim

WORKDIR /app

COPY docker/requirements.german.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV FLASK_ENV=production
ENV PORT=8767

EXPOSE 8767

CMD ["python", "domains/german/html_server.py"]
