FROM python:3.12-slim

WORKDIR /app

COPY docker/requirements.curator.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV FLASK_ENV=production
ENV PORT=8766

EXPOSE 8766

CMD ["python", "curator_server.py"]
