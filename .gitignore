ai-env/
__pycache__/
*.pyc
.DS_Store

# Credentials (NEVER COMMIT!)
.env
.flask_secret
credentials.json
secrets.json

# Curator output files (generated, not source)
curator_output.txt
curator_index.html
telegram_message*.txt
curator_archive/
# NOTE: curator_briefing.html and curator_latest.html are tracked (Flask-served templates).
# They are regenerated daily by the curator run — expect dirty git state after each run.

# Personal feedback data (contains preferences and feedback history)
curator_preferences.json
curator_errors.log

# Personal learning signals and cost data
signal_store.jsonl
signal_store.jsonl.bak
curator_costs.json

# Generated UI output files
curator_preview_recovered.html
server.log

# Personal deep dive content — personal analysis stays private, track only the index template
interests/ratings.json
interests/TEMPLATE.md
interests/2026-*.md
interests/2026/scans/*.md
interests/2026/scans/*.html
!interests/2026/scans/index.html

# Deep dive system (generated content)
curator_cache/
curator_history.json

# X Adapter — processed tweet ID cache (local state, not source)
x_adapter_cache.json
x_import_archive_cache.json
x_pull_state.json

# X data archive (personal bookmark data — never commit)
bookmarks.js
bookmarks.*.js
curator_latest_with_buttons.html

# Temporary/test files
curator_briefing_old.html
curator_briefing_table.html
html_button_snippet.html
test_feedback.sh
scripts/test_cron.sh

# Voice notes (personal thought capture)
VOICE_NOTES.md

# Logs (generated, not source)
logs/

# Claude Code session data
.claude/

# OpenClaw sync notes (transient handoff files)
.SYNC_NOTE.md

# Private planning docs (live in rvs-openclaw-agent)
ROADMAP_X_INTEGRATION.md

# Raw unblurred screenshots (use docs/screenshots/ for blurred versions)
Screenshot*.jpg
Screenshot*.png
curator_preview.txt

# Downloaded media — large binaries, personal reading signals
curator_media/

# New domains — research-intelligence now tracked on poc/research-source-quality
# _NewDomains/

# Private onboarding context — never commit
context/

# Working/scratch files — short-term, not for repo
_working/*
# Design/Dev agent: trash is local only, archive is committed via private remote
_working/trash/
# Build notes are permanent reasoning artifacts — commit them
!_working/build_note_*.md
!_working/BUILD_PLAN_*.md
!_working/BUILD_PLAN_*.md.pdf
# CLI and tool reference docs — permanent, commit them
!_working/CURATOR_RESEARCH_CLI_REFERENCE.md
# Confirmed design decision docs — permanent, commit them
!_working/CURATOR_DESIGN_DECISIONS_*.md
# STATE docs — on-disk reality snapshots, permanent record, commit them
!_working/CURATOR_STATE_*.md
# Feature specs — approved specs queued for build, permanent record, commit them
!_working/feature_*.md
# Build specs — approved specs queued for build, permanent record, commit them
!_working/spec_*.md
!_working/spec_*.md.pdf
# Handoff docs — design→build handoffs, permanent record, commit them
!_working/handoff_*.md
# Plan docs — design plans from OpenClaw/Claude.ai, permanent record, commit them
!_working/plan_*.md
!_working/plan_*.md.pdf
# Living roadmap doc — versioned source of truth, commit it
!_working/ROADMAP.md
# Approach docs — filed by dev agent, permanent record, commit them
!_working/approach_*.md
# Design docs — architecture/design artifacts, permanent record, commit them
!_working/design_*.md
!_working/design_*.md.pdf
# Build specs — prefixed variant from Claude.ai, permanent record, commit them
!_working/build_spec_*.md
!_working/build_spec_*.md.pdf

# Finance domain — private data
domains/finance/statements/
domains/finance/reports/

# Language learning — private data
domains/language_learning/languages/
domains/language_learning/*/sessions/
domains/language_learning/*/exercises/
domains/language_learning/*/vocabulary/

# Language German domain — personal data
_NewDomains/language-german/language/german/sessions/
_NewDomains/language-german/language/german/anki/
_NewDomains/language-german/language/german/lessons/
_NewDomains/language-german/language/german/progress.json

# Portal auth — credentials never in public repo (keep in mini-moi-private)
minimoi_portal/auth/users.json
minimoi_portal/auth/guests.json

# Guild domain — private data lives in mini-moi-private repo
_NewDomains/guild/job-search/**
!_NewDomains/guild/job-search/.gitkeep
_NewDomains/guild/new-ventures/job-search/**
!_NewDomains/guild/new-ventures/job-search/.gitkeep
!_working/curator-redesign/
!_working/curator-redesign/*.pdf
!_working/german-redesign/
!_working/german-redesign/*.pdf
domains/guild/data/linkedin_connections.csv
data/curator/
